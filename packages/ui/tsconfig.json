{
  "extends": "tsconfig/vue-library.json",
  "include": ["."],
  "exclude": [
    "dist",
    "build",
    "node_modules",
    // Needed to prevent volar from treating vue templates like jsx
    "**/*.stories.ts"
  ]
}
